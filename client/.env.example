# Base URL of the API when it is hosted on a different domain (leave empty for same-origin /api)
VITE_API_URL=
# "hash" for static hosts without SPA rewrites (URLs like /#/products/...), otherwise leave empty
VITE_ROUTER_MODE=
